1. acwing: 荷兰国旗问题， 分三个区间， 0-j, j-i, i-k,分情况讨论
class Solution {
    public void sortColors(int[] nums) {
        for (int i = 0, j = 0, k = nums.length-1; i <= k; ) {
            if (nums[i] == 0) {
                swap(nums, i, j);
                i++;
                j++;
            } else if (nums[i] == 2) {
                swap(nums, i, k);
                k--;
                //此时不用i++,因为不知道换过来的是什么数，还需要继续判断。
            } else {
                i++;
            }
        }
        
    }
    public void swap(int[] nums, int i, int j) {
        int tmp = nums[i];
        nums[i] = nums[j];
        nums[j] = tmp;
    }
}

2. 这道题的本质还是一道排序的题，题目中给出提示说可以用计数排序，需要遍历数组两遍，那么先来看这种方法，因为数组中只有三个不同的元素，所以实现起来很容易。

- 首先遍历一遍原数组，分别记录 0，1，2 的个数。
- 然后更新原数组，按个数分别赋上 0，1，2。
但是有2个pass. 
